# FeedbackOS 项目计划

## 1. 产品定义

### 一句话定位

FeedbackOS 是面向中小型 SaaS 与 AI 产品团队的反馈决策工作台：从评论、客服记录和表格中找出下周最值得解决的三个问题，并在版本发布后验证是否真的改善。

### 目标用户

- 5～50 人的 SaaS 或 AI 产品团队。
- 没有专职用户研究团队的创始人、产品经理或客服负责人。
- 每周或每个版本都会处理用户反馈，但主要依赖 Excel、群聊和人工阅读的团队。

### 核心问题

- 反馈散落在 Excel、客服、问卷、群聊和应用商店中。
- 团队只能看到零散案例，难以判断问题频率和影响范围。
- AI 总结缺少原文证据，无法支撑产品评审。
- 反馈被转成需求后，通常没有继续验证修复效果。

### 核心价值

```text
不是：帮用户读完一份反馈文件
而是：持续保留证据，推动产品决策，并验证决策效果
```

## 2. 首版用户旅程

1. 用户创建工作空间和产品项目。
2. 上传 CSV 或 Excel 反馈文件。
3. 在预览中映射正文、时间、版本、渠道、用户等字段。
4. 系统执行清洗、脱敏、去重和质量检查。
5. 系统对反馈进行语义聚类和主题解释。
6. 用户查看主题趋势，并下钻原始证据。
7. 系统给出可解释的 Top 3 问题建议。
8. 用户接受、修改或忽略建议，并关联到一个产品版本。
9. 新版本发布后导入下一批反馈。
10. 系统比较发布前后数据，判断问题是否改善。

## 3. MVP 功能范围

### P0：必须完成

- 邮箱注册/登录。
- 工作空间与项目。
- CSV、XLSX 文件上传。
- 字段映射和数据预览。
- 导入批次状态与错误报告。
- 文本清洗、个人信息脱敏、精确及近似去重。
- Embedding、聚类、主题命名。
- 主题列表、数量、趋势和原文证据。
- 可解释的 Top 3 问题。
- 用户修正主题与优先级。
- 产品版本及发布前后比较。
- Markdown/Excel 报告导出。
- 基础用量、日志、错误监控和 AI 调用成本记录。

### P1：完成 MVP 后评估

- GitHub Issue、飞书项目或 PingCode Webhook。
- 定时导入和重复分析。
- 公开或带密码的报告分享。
- 团队邀请和更细粒度权限。
- 自定义问题分类与优先级权重。

### 暂不实施

- 全网评论抓取。
- 实时客服平台连接器。
- 自动做产品路线图决策。
- 自动向用户发送通知。
- 移动 App、企业 SSO、私有化部署。
- 多模态音视频反馈。
- 模型训练和微调。

## 4. 优先级模型初稿

Top 3 不能来自不可解释的大模型评分。首版使用透明公式：

```text
priority = frequency × severity × growth × confidence × segment_weight
```

建议定义：

- `frequency`：问题影响的去重反馈数量或用户数量。
- `severity`：阻断、严重影响、一般摩擦、建议四档。
- `growth`：当前窗口相对上一窗口的变化。
- `confidence`：样本量、聚类一致性和证据完整度。
- `segment_weight`：目标客群、付费用户或关键版本的可配置权重。

大模型可以建议 `severity`，但必须提供理由和证据，最终允许用户修改。所有数量和变化由程序计算。

## 5. 技术方案

### Web

- Next.js + React + TypeScript。
- 响应式工作台、数据表格、趋势图、反馈原文抽屉。
- 使用 Server Components 和服务端数据获取的边界需在初始化时确定。

### API 与业务服务

- FastAPI + Pydantic。
- SQLAlchemy + Alembic。
- REST API 起步；长任务进度使用轮询或 SSE，首版不引入 WebSocket。

### 数据与任务

- PostgreSQL 保存业务数据。
- pgvector 保存反馈 Embedding。
- Redis + Celery 处理导入、Embedding、聚类和模型调用。
- S3 兼容对象存储保存原始文件，设置生命周期和删除策略。

### AI 分析管道

```text
parse
  → validate
  → normalize
  → redact
  → deduplicate
  → embed
  → cluster
  → label themes
  → calculate metrics
  → generate evidence-backed recommendations
```

每个阶段独立记录状态和版本，允许失败后从安全检查点恢复。

### 可观测与评测

- 结构化日志和 OpenTelemetry。
- 保存分析耗时、成功率、Token、模型成本、解析失败和重试。
- 维护脱敏、去重、主题分类和证据引用的固定评测集。

## 6. 八周上线计划

按单人每周约 15～20 小时估算。

### 第 1 周：用户与数据验证

目标：确认用户是否有重复分析需求，而非只需要一次性报告。

- 访谈 8～10 位产品经理、创始人或客服负责人。
- 获取至少 3 份经过授权和脱敏的真实反馈样本。
- 手工完成 3 份 Top 3 问题报告。
- 定义统一反馈字段和第一版问题分类。
- 建立产品落地页和内测登记入口。

验收：至少 3 位用户愿意在下一周或下一个版本再次提供数据。

### 第 2 周：产品骨架与文件导入

- 初始化 Web、API、数据库和本地容器环境。
- 完成注册、登录、工作空间和项目。
- 实现 CSV/XLSX 上传、预览与字段映射。
- 创建导入批次、文件存储和基础数据表。

验收：用户可以创建项目并成功预览三种不同结构的文件。

### 第 3 周：数据质量管道

- 文本标准化、空值和异常行处理。
- 手机、邮箱等个人信息脱敏。
- 精确去重和语义近似去重。
- Celery 任务状态、进度、错误和重试。
- 导入质量报告。

验收：同一文件重复上传不会产生重复反馈，失败任务可安全重跑。

### 第 4 周：AI 分析引擎 V1

- 生成和保存 Embedding。
- 相似反馈聚类。
- 大模型主题命名和问题摘要。
- 结论与 FeedbackItem 证据绑定。
- 支持主题合并、拆分和重命名。

验收：每个主题都能下钻到原始反馈；没有证据时不生成结论。

### 第 5 周：决策看板

- 主题数量、趋势和用户群筛选。
- 严重度、频率、增长率和置信度。
- 透明的 Top 3 优先级建议。
- 用户接受、修改、忽略和备注。
- 记录人工修正，用于评测。

验收：产品经理能在 10 分钟内从导入完成到产出一份可评审报告。

### 第 6 周：版本闭环

- 创建和管理产品版本。
- 将问题关联到计划修复版本。
- 设置发布前后分析窗口。
- 对比问题数量、占比、严重度和用户群变化。
- 导出 Markdown 与 Excel 报告。
- 选择一个外部任务系统完成最小 Webhook 集成。

验收：至少一个问题能够完成“证据→决策→版本→效果”的追踪。

### 第 7 周：评测与生产加固

- 创建 200～300 条人工标注测试数据。
- 评测脱敏、去重、分类、聚类和证据引用。
- 比较至少两组 Prompt 或模型方案。
- 增加超时、限流、重试、降级和每日成本上限。
- 接入链路追踪和异常监控。
- 验证工作空间数据隔离和文件删除。

验收：原文引用准确可追溯；关键安全与权限测试通过；成本可被统计和限制。

### 第 8 周：内测与上线

- 邀请 5～10 位设计合作用户。
- 修复阻断使用的高频问题。
- 创建 2～3 份不含内部信息的公开演示数据。
- 发布版本前后反馈变化案例。
- 以人工参与的 ¥199～499 分析服务测试付费。
- 准备用户协议、隐私说明、数据删除说明和 AI 输出提示。

上线判断：

- 至少 3 位用户完成两次分析。
- 至少 1 位用户愿意付费。
- 至少 1 条建议被转成真实产品任务。
- 至少完成 1 次发布前后效果验证。

若未达到重复使用，应先调整用户和场景，不继续扩大功能。

## 7. 评测计划

首版评测集覆盖：

- 手机号、邮箱、姓名等敏感信息。
- 完全重复、改写重复和相似但不同的问题。
- 一条反馈包含多个问题。
- 中英文混合、口语、错别字、表情和反讽。
- 客服回复与用户原话混杂。
- 样本不足、观点冲突和少数高严重度问题。

核心指标：

- 脱敏召回率和误伤率。
- 重复识别精确率与召回率。
- 主题分类准确率。
- 聚类一致性与人工接受率。
- 原文证据引用正确率。
- Top 3 建议人工接受率。
- 单次分析耗时、成本和失败率。

## 8. 商业验证

### 第一阶段：服务辅助

- 免费提供少量样本分析。
- 完整分析报告按次测试 ¥199～499。
- 人工参与，以发现最值得自动化的环节。

### 第二阶段：轻量订阅

- 个人版测试 ¥39～99/月。
- 团队版测试 ¥299/月起。
- 套餐围绕项目数、反馈条数、分析次数和成员数设计。

价格均为验证锚点，不视为已确认市场价格。至少出现 3 个真实付费用户后，再开发完整支付体系。

## 9. 主要风险

| 风险 | 应对策略 |
|---|---|
| 用户直接使用 Codex 完成一次性分析 | 聚焦持续数据、版本对比和团队闭环 |
| 飞书、PingCode 增加类似 AI 能力 | 做轻量、跨工具的证据与验证层，不替代研发管理 |
| AI 聚类不稳定 | 保留固定分类、人工修正、版本化与回归评测 |
| 缺少真实数据 | 第 1 周先获得设计合作用户，禁止用虚假需求自我验证 |
| 用户数据敏感 | 最小化采集、默认脱敏、隔离存储、可彻底删除 |
| 单人范围失控 | 只完成主闭环，所有连接器和高级协作后置 |
| 模型成本不可控 | 分层处理、缓存、批量调用、用量限制和成本监控 |

## 10. 下一步

当前仅完成项目目录、协作规范和计划。等待用户迁移进入 `feedbackos` 目录并明确开始后：

1. 检查项目运行环境和包管理器。
2. 确认是否采用 Web/API/Worker 分离结构。
3. 创建项目脚手架与本地 Docker 开发环境。
4. 从第 1 周的用户与数据验证任务开始，不提前堆叠功能。
