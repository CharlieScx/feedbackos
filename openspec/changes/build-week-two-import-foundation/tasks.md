## 1. 固定依赖与初始化工程

- [x] 1.1 从各项目官方发布源核验 Next.js、React、TypeScript、Ant Design、FastAPI、Pydantic、SQLAlchemy、Alembic、PostgreSQL、Garage、Polars 和 openpyxl 的当前稳定版本，记录版本与选择理由，并验证记录中的版本能被对应包管理器解析
- [x] 1.2 初始化 `apps/web`、`apps/api`、`packages/contracts` 和 `infra` 的最小目录、运行时版本文件与锁文件，并验证全新检出后依赖安装不产生未锁定版本漂移
- [x] 1.3 配置 PostgreSQL 与 Garage 的 Compose 服务、健康检查、命名卷和私有开发 bucket 初始化，并验证一次命令启动后两个服务均达到 healthy
- [x] 1.4 增加分层配置、`.env.example`、密钥校验和统一的开发/迁移/测试命令，并验证缺失生产密钥时快速失败且示例文件不包含真实凭据
- [x] 1.5 建立 Next.js 与 FastAPI 健康检查及 Web 同源 `/api` 代理，并通过浏览器侧请求验证 API 可达且不需要跨域 Cookie 配置

## 2. 建立 UI 主题与 API 契约

- [x] 2.1 将权威 `design-tokens.json` 映射为项目内浅色/深色主题、Ant Design `ConfigProvider` 和语义 CSS/Tailwind 变量，并通过主题单元测试与生产构建验证业务组件不依赖技能包路径或硬编码视觉值
- [x] 2.2 搭建认证页、工作空间/项目壳层和项目导入页的路由骨架，使用主题间距、圆角、字号与状态 token，并通过键盘导航和浅色/深色截图检查验证基础布局
- [ ] 2.3 从 FastAPI OpenAPI 输出生成 `packages/contracts` 的 TypeScript 类型与 API 客户端边界，并通过 CI 漂移检查验证后端契约变化会使未更新的前端类型检查失败
- [ ] 2.4 定义统一成功响应、分页和安全错误结构，验证 Web 可按错误代码区分认证失败、资源不存在、文件无效、幂等冲突与可重试失败

## 3. 实现数据模型、会话与租户边界

- [ ] 3.1 实现数据库连接、事务边界和 `User`、`Session`、`Workspace`、`Member`、`Project`、`DataSource`、`StoredFile`、`ImportBatch`、`FeedbackItem` 模型，并通过模型测试验证租户外键、唯一约束和状态枚举
- [ ] 3.2 创建首个 Alembic 迁移并增加迁移测试，验证空 PostgreSQL 可升级到最新、降级后可再次升级，且不产生无法归属工作空间的项目或批次记录
- [ ] 3.3 实现邮箱规范化、密码规则、Argon2id 哈希、不透明会话签发/哈希/过期/撤销服务，并通过单元测试验证重复邮箱、错误密码、过期会话和退出失效
- [ ] 3.4 实现注册、登录、当前用户和退出 API，配置 HttpOnly/SameSite/Secure Cookie 与修改请求 Origin 校验，并通过 API 测试验证凭据不进入响应、日志或前端脚本
- [ ] 3.5 实现从当前成员关系限定的工作空间与项目创建、列表和详情 API，并通过双用户集成测试验证 owner 创建、按租户过滤、未认证拒绝与跨工作空间统一不存在响应
- [ ] 3.6 接入 Web 注册/登录、会话恢复、退出、工作空间创建和项目创建界面，并通过组件测试验证加载、校验、失败恢复和成功跳转状态

## 4. 实现文件存储、解析与批次状态机

- [ ] 4.1 实现只暴露共同 S3 子集的对象存储适配器、工作空间/项目分区键和最小权限配置，并通过 Garage 集成测试及 OSS 专用 bucket 契约测试验证跨前缀访问被拒绝且 API 不返回内部对象键或凭据
- [ ] 4.2 实现 multipart 流式临时文件写入、大小计算、SHA-256、扩展名/签名校验和必执行清理，并通过超限、伪装扩展名、损坏文件及中断上传测试验证无残留临时文件
- [ ] 4.3 实现 CSV 受控编码/分隔符识别、前导空行与表头处理、总行数和有界样本预览，并通过 UTF-8、UTF-8 BOM、GB18030、乱序列及截断夹具单元测试
- [ ] 4.4 实现 XLSX 只读解析、第一个非空工作表选择、总行数和有界样本预览，并通过空首表、多工作表、损坏工作簿与大于预览上限夹具单元测试
- [ ] 4.5 实现批次 `uploaded`、`preview_ready`、`mapping_confirmed`、`failed` 状态转换与安全错误元数据，并通过状态机测试验证非法跳转、可重试失败和映射确认的原子性
- [ ] 4.6 实现带客户端幂等键的上传事务、私有对象写入、批次创建和数据库失败补偿清理，并通过重放、同键不同哈希、对象失败和事务失败集成测试验证只产生一个批次/对象
- [ ] 4.7 实现批次列表、状态/预览读取和失败后重新预览 API，并通过集成测试验证稳定排序、复用原对象、跨租户拒绝和失败后不展示伪成功状态
- [ ] 4.8 实现字段映射校验、版本化保存和确认 API，并通过测试验证正文必填、源列唯一、不存在列、并发旧版本冲突，以及确认后 `FeedbackItem` 数仍为零
- [ ] 4.9 为上传与解析增加只含标识符、状态、耗时和安全统计的结构化日志，并通过日志捕获测试验证反馈原文、样本单元格、Cookie、密码及存储凭据均未出现

## 5. 完成上传、预览与字段映射界面

- [ ] 5.1 实现项目导入页的上传步骤，展示来自 API 的类型/大小限制、上传进度和幂等重试，并通过组件测试验证重复提交复用请求标识且错误可恢复
- [ ] 5.2 实现预览步骤的表头、有限样本表格、总行数、截断标记、编码/工作表和警告展示，并通过 CSV/XLSX 组件夹具验证不会误显示为已导入
- [ ] 5.3 实现 `feedback_text` 必填与四个可选字段的一对一映射交互、冲突提示和映射版本提交，并通过组件测试验证缺失正文、重复源列和过期版本无法确认
- [ ] 5.4 实现项目批次列表和 `uploaded`、`preview_ready`、`mapping_confirmed`、`failed` 状态反馈，验证可重试失败提供明确操作且映射确认显示“等待质量管道、已导入 0 条”
- [ ] 5.5 对认证、工作空间、项目和三步导入流执行键盘、焦点、表单标签、状态播报及浅色/深色检查，并通过自动化可访问性测试与两套主题构建

## 6. 验收第二周纵向闭环

- [ ] 6.1 创建不含真实客户信息的中文乱序列 CSV、缺少可选列的英文 CSV 和首个非空工作表为数据表的 XLSX 合成夹具，并通过解析单元测试核对预期表头、行数、样本和映射
- [ ] 6.2 增加 PostgreSQL + Garage 集成测试套件，并为具备凭据的环境增加 OSS 存储契约测试，验证注册到映射确认的数据库/对象一致性、上传幂等性、补偿清理、批次重试、双工作空间隔离及云端兼容性
- [ ] 6.3 增加 Playwright 主链路测试，依次完成注册、创建工作空间、创建项目及三种文件的上传/预览/映射，并验证三个独立批次均为 `mapping_confirmed` 且已导入反馈数为零
- [ ] 6.4 运行类型检查、Lint、API 单元/集成测试、Web 组件测试、端到端测试、OpenAPI 漂移检查、Alembic 迁移检查和生产构建，并保存全部通过的命令摘要
- [ ] 6.5 编写本地启动、环境变量、迁移、三文件手工验收和故障清理说明，并按文档从空环境复跑一次以验证新贡献者可以完成第二周验收
- [ ] 6.6 记录第二周验收结论与已知限制，明确第一周访谈和真实样本门槛仍未完成且不由本次工程结果替代，并验证未修改或伪造 `validate-week-one-demand` 的验收数据
