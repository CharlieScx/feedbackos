# FeedbackOS 项目协作规范

## 项目目标

FeedbackOS 面向 5～50 人的 SaaS 与 AI 产品团队，持续回答两个问题：

1. 下周最应该解决哪三个用户问题，为什么？
2. 某次版本发布后，这些问题是否真的得到改善？

本项目既要成为可供真实用户使用和付费的产品，也要完整展示从前端到全栈、数据工程和 AI 应用工程的能力。

## 产品边界

首个可用版本只实现以下闭环：

```text
上传反馈 → 清洗与脱敏 → 主题分析 → 原文证据 → Top 3 问题
         → 关联产品版本 → 发布前后对比 → 导出或创建任务
```

首版不做：

- 通用聊天机器人。
- 自动决定产品路线图。
- 自动修改代码或直接发布任务。
- 全量接入所有客服、舆情和社交平台。
- 企业 SSO、私有化部署或移动 App。
- 多智能体展示性编排。
- 模型训练或微调。
- 没有真实需求支撑的复杂计费系统。

## 核心工程原则

### 证据优先

- 每个 AI 结论必须关联一个或多个原始反馈 ID。
- 用户必须能从主题、结论和优先级下钻到原文。
- 没有证据时不得生成确定性结论。
- 必须保留反面证据、少数意见和不确定性，不得只展示支持结论的数据。

### 确定性计算与 AI 分工

- 数量、比例、趋势、版本差异由 SQL 或确定性程序计算。
- 脱敏、字段校验、精确去重优先使用规则和程序。
- Embedding 用于语义相似度和候选聚类。
- 大模型用于主题命名、问题解释、摘要和行动建议。
- 优先级必须使用透明、可解释、可被用户修改的计算方式。
- 不允许让大模型凭空计算统计数字或伪造用户原话。

### 隐私与安全

- 默认仅处理用户主动上传或明确授权接入的数据。
- 原始文件进入分析前执行敏感信息识别和脱敏。
- 日志不得记录原始反馈全文、访问令牌、Cookie、密码或模型密钥。
- 工作空间之间必须进行严格的数据隔离与权限校验。
- 删除项目时需要同步处理原始文件、派生数据和向量数据。
- 任何对外分享的报告默认隐藏个人身份信息。

### 可重复与可评测

- 每次分析保存模型、Prompt、Embedding、规则和数据版本。
- 同一导入批次重复执行必须具备幂等性。
- Prompt 或模型变更前运行固定评测集，记录质量、成本和延迟变化。
- 分析失败必须可重试，不得生成半完成但看似成功的报告。
- 用户的合并、拆分、重命名和优先级修正需要被记录，用于后续评测。

## 计划技术架构

在正式初始化代码前，再核验依赖的当前稳定版本。总体技术方向如下：

- Web：Next.js、React、TypeScript。
- API：Python、FastAPI、Pydantic、SQLAlchemy、Alembic。
- 数据库：PostgreSQL；语义向量使用 pgvector。
- 异步任务：Redis 与 Celery Worker。
- 文件：本地开发使用兼容 S3 的对象存储，生产环境可切换 OSS/COS。
- 数据处理：Polars、openpyxl、scikit-learn。
- 可观测：结构化日志、OpenTelemetry Trace/Metrics、模型调用记录。
- 部署：Docker Compose 起步，Web、API、Worker、数据库逻辑分离。

预期目录结构：

```text
feedbackos/
├── apps/
│   ├── web/
│   └── api/
├── services/
│   └── worker/
├── packages/
│   ├── contracts/
│   └── evals/
├── infra/
├── docs/
├── AGENTS.md
└── PROJECT_PLAN.md
```

目录仅是目标结构；开始编码前根据脚手架和部署方式确认，不要为了符合目录而制造无用包。

## 数据模型约束

核心实体至少包括：

- Workspace、Member、Project。
- DataSource、ImportBatch、FeedbackItem。
- Customer/Cohort（首版允许匿名或为空）。
- AnalysisRun、Theme、Evidence、PriorityScore。
- Decision、Release、ReleaseComparison。
- ModelCall、PromptVersion、UserCorrection。

所有派生结论必须能追溯到 `AnalysisRun`、数据批次和原始 `FeedbackItem`。

## 开发工作方式

- 中文优先设计产品文案和默认反馈分类，代码标识符使用英文。
- 先完成一条端到端闭环，再增加数据源和分析维度。
- 保持改动范围小；不要顺手重构或格式化与当前任务无关的文件。
- 优先复用仓库已有组件、设计 token、工具函数和基础设施。
- 新增依赖前说明必要性，避免功能重叠的大型框架。
- AI 工作流优先使用清晰的阶段式管道；首版不因“Agent”概念引入复杂编排框架。
- 不把用户提供的内部数据、真实客户信息或公司材料写入示例和测试夹具。
- 演示数据使用人工构造、明确授权或可合法使用的公开数据，并注明来源和边界。
- 测试文件可以长期保留在本地，但必须统一放在仓库根目录 `tests/` 下，并按 `tests/api`、`tests/web`、`tests/e2e` 等子目录分类；不得与 `apps/**/src` 等业务逻辑代码混放，也不得提交到 Git。

## 验证要求

每个功能完成后，至少验证与改动相称的内容。以下自动化测试在本地 `tests/` 目录中执行，测试代码和夹具不进入 Git：

- 数据清洗、脱敏、去重和优先级：单元测试。
- API、权限和任务幂等性：集成测试。
- 上传到查看报告的主链路：端到端测试。
- AI 输出：固定数据集回归评测，不以单次肉眼检查代替。
- 提交前运行类型检查、Lint、相关本地测试（若存在）和构建。

首版关键质量门槛：

- 所有展示的原文引用都能准确回到对应反馈。
- 统计数字由确定性程序产生并可复算。
- 相同文件重复导入不会生成重复反馈。
- 未授权成员无法读取其他工作空间数据。
- 任务失败时用户能看到状态并安全重试。

## 商业验证原则

- 在复杂支付系统之前，先验证真实用户是否重复使用和愿意付费。
- 早期允许以人工参与的分析服务验证价值，但必须记录哪些步骤值得产品化。
- 核心指标优先级：重复使用 > 决策被采纳 > 问题得到改善 > 注册量。
- 如果用户只进行一次性分析且不再回来，应重新评估定位，而不是继续堆功能。

## 当前状态

项目目前只保存规范和计划，尚未初始化代码。等待用户迁移进入该目录并明确开始后，再创建脚手架或实现功能。
